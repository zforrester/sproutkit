---
layout: '../../layouts/Article.astro'
title: 'Typography'
description: 'Intro'
---

import TypographyFontFamilies from '@components/TypographyFontFamilies.astro'
import TypographyFontSizes from '@components/TypographyFontSizes.astro'
import TypographyFontWeights from '@components/TypographyFontWeights.astro'
import TypographyLineHeights from '@components/TypographyLineHeights.astro'
import TypographyLetterSpacing from '@components/TypographyLetterSpacing.astro'

## Set up

Typographic properties are defined in `tokens/typography.json`. These propertiers include `sizes`, `families`, `weights`, `line-heights`, and `letter-spacing`. You can modify these property definitions or replace them with your own.

## Usage

The properties can accessed by using a function name that matches the CSS property name.

```
.headline {
	font-size: font-size();
	font-family: font-family();
	font-weight: font-weight();
	line-height: line-height();
	letter-spacing: letter-spacing();
}
```

Alternatively, there is a `@mixin font()` that provides a short cut for these propeties.

```
// SCSS
.headline {
  @include font(32, 'bold', 'primary');
}

// CSS
.headline {
	font-size: 32px;
	font-weight: 600;
	font-family: 'Avenir Next';
}
```

## Tokens

### Font Families

<TypographyFontFamilies />

### Font Sizes

<TypographyFontSizes />

### Font Weights

<TypographyFontWeights />

### Line Heights

<TypographyLineHeights />

### Letter Spacing

<TypographyLetterSpacing />
