<div class='topbar'>
  <div class='logo-container'>
    <div class='logo'></div>
    <span class='name'>Sproutkit</span>
  </div>
</div>

<style lang='scss'>
  .topbar {
    width: 100%;
    padding: space(2);
    z-index: 1;
    position: sticky;
    top: 0;
    left: 0;
    right: 0;
    // background: color('cool-gray-100');
    // box-shadow: 0 1px 0 color('cool-gray-500', 0.25);

    overflow-anchor: none;
    isolation: isolate;
    min-height: 0;
    // @supports (-webkit-backdrop-filter: blur(0)) {
    //   -webkit-backdrop-filter: blur(40px) brightness(120%);
    //   backdrop-filter: blur(40px) brightness(120%);
    //   background: linear-gradient(to bottom, color('white', 0.6), color('white', 0.2));
    // }
  }

  .logo-container {
    display: flex;
    align-items: center;
    gap: space(1.5);
    // position: relative;
    // top: space(5);

    .logo {
      @include aspect-ratio(1, 1);
      width: space(3);
      border-radius: 6px;
      box-shadow: inset 0 0 0 2px color('black'), 0 0 0 2px color('black');
    }
    .name {
      @include font(16, 'bold');
      color: color('black');
    }
  }
</style>
